{
  "name": "capitol-index-build",
  "version": "1.0.0",
  "description": "Build scripts for The Capitol Index — fetches public API data and bakes it into the site",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch:members":   "node fetch-members.js",
    "fetch:trades":    "node fetch-trades.js",
    "fetch:finance":   "node fetch-finance.js",
    "fetch:lobbying":  "node fetch-lobbying.js",
    "build":           "node build.js",
    "dev":             "npm run fetch:members && npm run fetch:trades && npm run fetch:finance && npm run fetch:lobbying && npm run build"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18"
  }
}
